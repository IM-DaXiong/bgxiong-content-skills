{
  "generationOwnership": "shape-only; camera semanticVocabulary is projected at runtime from cinematic_language::schema_gen",
  "shots": {
    "count": { "min": 1, "max": 24 },
    "item": {
      "required": ["shotIndex", "content"],
      "properties": {
        "shotIndex": { "type": "integer", "minimum": 1, "maximum": 24 },
        "content": { "type": "string", "minLength": 8, "maxLength": 2000 },
        "angleId": { "type": "string" },
        "shotSize": { "type": "string" },
        "cameraMove": { "type": "string" },
        "beat": { "type": "string", "maxLength": 200 },
        "lens": { "type": "string" },
        "durationHint": { "type": "string" }
      }
    }
  }
}
